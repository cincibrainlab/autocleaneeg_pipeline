{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python: UV Run",
            "type": "debugpy",
            "request": "launch",
            "program": "${file}",
            "console": "integratedTerminal",
            "env": {
                "PATH": "${env:PATH}:/Users/ernie/.cargo/bin/"  // Adjust this path to where uv is installed
            },
            "args": [],
            "justMyCode": true,
            "python": "${command:python.interpreterPath}",
            "preLaunchTask": "uv run"
        }
    ]
}
